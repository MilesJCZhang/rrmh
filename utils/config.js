// utils/config.js - 全局配置中心
// ⚠️  所有环境开关、DEV 标志、版本号统一在此文件管理
// 上线时只需改此文件，无需到处搜索散落的 const

// ======================================================
// 1. 环境标识
// ======================================================
// 'dev'  - 本地开发（使用 Mock 数据，DEV_MODE 身份切换可见）
// 'prod' - 生产上线（关闭 Mock，关闭身份切换面板）
const ENV = 'prod';  // ← 已切换到生产环境

const IS_DEV = ENV === 'dev';
const IS_PROD = ENV === 'prod';

// ======================================================
// 2. API 基础地址
// ======================================================
const API_BASE_URL_MAP = {
  dev: 'http://192.168.3.7:3000',
  prod: 'https://rrmhdate.cn',
};
const API_BASE_URL = API_BASE_URL_MAP[ENV];

// ======================================================
// 3. DEV_MODE 身份切换开关（上线前改 IS_DEV 即可）
// ======================================================
const DEV_MODE = false;              // 上线前必须设为 false
const DEV_MOCK_DATA = false;        // 上线前必须设为 false

// ======================================================
// 4. 功能开关（灰度/分阶段上线用）
// ======================================================
const FEATURES = {
  SALON_ENABLED: true,             // 沙龙活动模块
  COMMUNITY_STATION_ENABLED: true, // 社区服务站模块
  AI_AVATAR_ENABLED: true,         // AI 画像模块
  CHAT_ENABLED: false,             // 聊天（二期）
  SMART_MATCH_ENABLED: false,      // 智能推荐（二期）
};

// ======================================================
// 5. 分页默认值
// ======================================================
const PAGE_SIZE = 20;

// ======================================================
// 6. 超时配置（ms）
// ======================================================
const REQUEST_TIMEOUT = 8000;
const UPLOAD_TIMEOUT = 30000;

// ======================================================
// 7. 小程序版本号（单一数据源，发版时只改这里）
// ======================================================
const VERSION = 'v1.0.13';

module.exports = {
  ENV,
  IS_DEV,
  IS_PROD,
  API_BASE_URL,
  DEV_MODE,
  DEV_MOCK_DATA,
  FEATURES,
  PAGE_SIZE,
  REQUEST_TIMEOUT,
  UPLOAD_TIMEOUT,
  VERSION,
};
