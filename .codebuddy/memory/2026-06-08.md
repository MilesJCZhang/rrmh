# 2026-06-08 工作日志

## 多角色身份切换修复

### 问题
用户"张进铖Miles"（ID=3，微信号miles202212）有两种推荐码类型：
- `city_partner` → 角色 `city_franchisee`（城市合伙人）
- `creator` → 角色 `partner_matchmaker`（联创推荐官）

但前端"我的"页面没有显示身份切换功能。

### 原因
1. **后端 `wechatLogin`/`userLogin` 返回字段名不匹配**：后端返回 `users` 字段，但前端 `app.js` 第211行读取 `data.user`，导致 `syncUserData` 未被调用，`roleList` 未被存储
2. **后端未返回 `roleList`**：`wechatLogin`、`userLogin`、`getUserInfo` 接口没有返回 `roleList` 数组

### 修复内容（生产服务器 dist/controllers/authController.js）

1. **添加 `__getUserRoles` 辅助函数**（行47-65）：动态从 `referral_codes` 表查询用户的推荐码类型，映射为角色列表
2. **`getUserInfo`**：在返回数据中添加 `roleList`
3. **`userLogin`**：添加 `roleList` 计算并返回，并将 `users` 字段改为 `user`
4. **`wechatLogin`**：添加 `roleList` 计算并返回，并将 `users` 字段改为 `user`

### `switch-role` 端点修复（dist/routes/user.js）
- 改为动态计算 `roleList`（从 `referral_codes` 表查询），不再依赖数据库 `users.roleList` Json 字段

### 角色计算逻辑
参考 `constants/roles.js` 中的角色定义，从 `referral_codes` 表的 `codeType` 字段映射：
- `public_welfare` → `public_matchmaker`
- `creator` → `partner_matchmaker`
- `city_partner` → `city_franchisee`
- 等

### 第二次修复（添加 single 角色 + roles 字段兼容）

**问题补充**：
- `roleList` 只包含推荐码映射的角色（`city_franchisee`、`partner_matchmaker`），不包含 `single`（单身会员）
- 用户之前是 `single`，成为推荐官后 `role` 被覆盖为 `partner_matchmaker`
- 用户中心（`user.js`）和工作台（`workbench.js`）使用 `userInfo.roles` 字段，但 `syncUserData` 只存了 `roleList`

**修复内容**：

1. **后端 `__getUserRoles` 添加 `single` 判断**：已建档用户（有 phone + nickname）自动拥有 `single` 角色
2. **后端 `switch-role` 同步更新**：同样的 `single` 角色判断逻辑
3. **前端 `services/auth.service.js` syncUserData**：`roleList` 同步到 `roles` 字段，兼容用户中心/工作台

现在用户张进铖Miles 的 `roleList = ['city_franchisee', 'partner_matchmaker', 'single']`

### 第三次修复（/v1/user/profile + 建档推荐人检查）

**问题1**：`/v1/user/profile` 端点（`dist/routes/user.js` 第55行）有 `users` 变量名错误（应为 `user`），且没有动态计算 `roleList`。
- 修复：`{ ...users, member }` → `{ ...user, roleList, member }`，添加动态 roleList 计算

**问题2**：切换到单身会员后，`mine.js` 的建档入口检查 `hasReferrer`，但推荐官自己不需要推荐人。
- 修复：`if (!this.data.hasReferrer)` → `if (!this.data.hasReferrer && !this.data.isMatchmakerRole)`

### 备份
2026-06-08 11:06 创建完整备份：`miniprogram_backup_20260608_110623.tar.gz`（3.8MB，排除 node_modules/memory）

---

## 编辑我的档案——成为会员支付改造 + 支付系统修复

### register.js 支付接口复用
- 将 register.js 的「成为会员」支付从模拟代码改为复用 `utils/payment.payRegistration()`，与 mine.js 完全一致
- 移除废代码 `onPaymentSuccess`，简化 `onConfirmPayment`
- `memberPrice` 从 99 改为 0.19（与支付系统一致）
- 弹窗增加限时特惠文案

### routes_payment.js 500 错误修复
**问题**：`POST /v1/payment/create` 返回 500 `"创建支付订单失败"`
**根因**：
1. `const logger = require('./utils/logger')` 定义在第 274 行（notify 回调内），但 `/create` 路由第 79 行起就用了 `logger.debug/error`，导致 logger 为 undefined
2. `orders` 表在 SQLite 数据库中不存在——建表脚本使用 MySQL 语法（`ENGINE=InnoDB`、`ENUM`），服务器实际跑 SQLite

**修复**：
1. `const logger = require('./utils/logger')` 移到文件顶部（第 14 行）
2. 在 `/create` 路由中通过 `CREATE TABLE IF NOT EXISTS` 自动创建 `orders` 表（SQLite 兼容：`INTEGER PRIMARY KEY AUTOINCREMENT`、`TEXT` 替代 `VARCHAR/ENUM`）
3. 同时创建 `commission_failures` 表
4. 清理底部冗余的 `ensureOrdersTable` 和 `setTimeout`

**修改文件**：
- `pages/register/register.js` — 支付接口复用改造 + 会员状态检查改用 `getProfile()` 替代不存在的 `/v1/user/member/status`
- `pages/register/register.wxml` — 价格/文案更新
- `pages/register/register.wxss` — 新增优惠提示样式
- `routes_payment.js` — logger import 位置 + orders 表自动建表

---

## 生产服务器部署（支付系统修复）

### 发现问题
之前修改的 `routes_payment.js` 是本地开发服务器的文件（Node.js/SQLite），生产服务器实际运行的是 TypeScript 项目（Prisma/MySQL，入口 `dist/index.js`）。生产服务器的 `dist/routes/payment.js` 没有 `MOCK_PAYMENT` 检查，始终调用真实微信支付 API。

### 部署内容
1. **`dist/routes/payment.js`**（服务器）— 在 `/create` 路由中插入 `MOCK_PAYMENT` 检查，当 `MOCK_PAYMENT=true` 时跳过微信支付，返回模拟成功
2. **`src/routes/payment.ts`**（服务器）— 同步更新 TypeScript 源码
3. **`.env`**（服务器）— 添加 `MOCK_PAYMENT=true`
4. **PM2 重启** — `pm2 restart renrenmeihao-api --update-env` 使修改和环境变量生效

### 追加修复：`actual_amount` 变量名错误
**错误日志**：`ReferenceError: actual_amount is not defined at payment.ts:199`
**根因**：TypeScript 源码中变量声明为 `let actualAmount = amount;`（camelCase），但后续使用 `actual_amount`（snake_case），编译后 JS 同名错误导致运行时报错。
**修复**：`dist/routes/payment.js` 和 `src/routes/payment.ts` 中所有 `actual_amount` 替换为 `actualAmount`（4处）。

### 追加修复：前端 `utils/payment.js` 模拟支付响应兼容
**问题**：服务器返回模拟支付成功（`{ mock: true, status: 'paid' }`），但前端 `createPayment` 仍调用 `wx.requestPayment()`，因缺少 `nonceStr/package/paySign` 导致支付失败。
**修复**：在 `createPayment` 中添加 `orderData.mock || orderData.status === 'paid'` 判断，跳过 `wx.requestPayment`，直接返回 `{ success: true }`。

### 追加修复：apply.ts 缺少 updated_at 字段
**问题**：`POST /v1/apply/public-matchmaker` 返回 500，错误为 `PrismaClientValidationError: Argument updated_at is missing`。
**根因**：`recommend_officers` 表的 Prisma 模型要求 `updated_at` 必填，但 `apply.ts` 中三个 create 调用（public/partner/professional）都未传入。
**修复**：`dist/routes/apply.js` 和 `src/routes/apply.ts` 中三个 create 调用加上 `updated_at: new Date()`，PM2 重启。

### 备份
2026-06-08 12:51 创建备份：`miniprogram_backup_20260608_125129.tar.gz`（3.8MB，排除 node_modules/memory）

---

## 后端修复：公益推荐官申请逻辑

### 问题
用户"张进铖Miles"（ID=3）角色为 `partner_matchmaker`，但数据库存在 `type='public'` 的申请记录，`POST /v1/apply/public-matchmaker` 返回 400 "您已申请过公益推荐官"。

### 修复（服务器 dist/routes/apply.js）
1. **检查顺序调整**：先检查用户角色（是否已是推荐官），再检查申请记录
2. **支持所有推荐官类型**：`public_matchmaker` / `partner_matchmaker` / `professional_matchmaker` 都算已认证
3. **已认证用户直接返回成功**（200），不再报错
4. **删除旧的未通过申请记录**，允许重新申请

### 版本升级到 v1.0.10
- 更新 5 个文件：`package.json`、`package-backend.json`、`admin-panel/package.json`、`utils/config.js`、`routes_config.js`
- 更新 `CHANGELOG.md`，创建 `RELEASE-v1.0.10.md`
- 创建并更新备份（含版次号）：14:18/14:19/14:37

---

## 前端修复：定时器崩溃 + referralService 未定义

### 1. 定时器内存泄漏（14:10）
- `pages/avatar/avatar.js`：`_startRecordTimer()` + `_startProgressPolling()` 添加 `if (!this || !this.data) return;`
- `pages/verify/verify.js`：`_startPolling()` 添加页面有效性检查
- `subpackages/user/pages/verify/verify.js`：同上

### 2. referralService 未定义（14:27）
- `pages/register/register.js`：缺少 `const referralService = require(...)` 导入，导致保存档案时报错 `Can't find variable: referralService`
- 修复：在第 8 行添加导入语句

### 备份
2026-06-08 14:37 创建备份（v1.0.10）：`miniprogram_v1.0.10_20260608_143710.tar.gz`（7.1MB，排除 node_modules）

---

## 前端修复：上传图片失败（字段名 + 上传类型错误）

### 问题
建档过程中，认证资料（身份证）和资产证明无法上传。
前端 `utils/request.js` 的 `uploadFile()` 硬编码 `name: 'avatar'`，但后端 `/image` 端点期望字段名 `'image'`，`/voice` 端点期望 `'voice'`。
同时 `register.js` 传了不存在的上传类型 `'id_card'` 和 `'asset'`，后端只支持 `avatar/image/voice`。

### 修复
1. **`utils/request.js`**：`name` 字段根据 type 动态选择：`{ avatar: 'avatar', image: 'image', voice: 'voice' }`
2. **`pages/register/register.js`**：身份证上传 `'id_card'` → `'image'`，资产证明 `'asset'` → `'image'`

---

## 推荐码绑定 400 错误修复（多次迭代）

### 问题
每次启动开发者工具都出现 `POST /v1/referral/bind 400 {message: "推荐码无效"}`，因为 scene 中包含了无效的 `referrerId`，`_handleScene` 将其存入 Storage，`login()` 回调读取后重复发起绑定请求。

### 修复过程（共 4 次）
1. **14:49** — `app.js` `bindByCode` 的 `.catch()` 中清除 `invitation_code` Storage
2. **14:49** — `app.js` `_handleScene` 先清除旧数据再存当前 scene，添加 `bindReferrer` 的 `.catch()` 清理
3. **15:06** — `app.js` `_handleScene` 用独立标志位 `_lastSceneReferrerId` 去重，不受 `login()` 回调清除 `pendingReferrerId` 的影响
4. **15:13** — `utils/referral.js` `bindReferrer`/`bindByCode` 的 `.catch()` 中 `console.error` 受 `silent` 控制，静默模式不再输出错误日志
5. **15:18** — `utils/referral.js` 添加 `SILENT_FAILED_IDS` 内存缓存，同一 session 中对同一无效 ID 只发一次请求
6. **15:21（最终版）**：
   - `utils/referral.js`：将 `SILENT_FAILED_IDS` 改为 Storage 持久化（`__silent_failed_referrers`），跨 session 有效
   - `app.js` `_handleScene`：**开发者工具环境直接 return**，不处理 scene 中的推荐人数据，彻底杜绝测试 scene 导致的无意义绑定请求

### 最终效果
- 开发者工具环境：`_handleScene` 直接返回，`pending_referrer_id` 不会被存储
- 真机环境：失败 ID 持久化到 Storage，同一 ID 只尝试绑定一次

---

## profile 页面缺少事件方法修复（15:11）

### 问题
渲染层报错：`Component "pages/profile/profile" does not have a method "onLogin"/"onGoRegister"`

### 修复
在 `pages/profile/profile.js` 中添加两个方法：
- `onLogin()`：触发 `app.login()`
- `onGoRegister()`：跳转 `/pages/register/register`

---

## 备份（最新）
2026-06-08 15:26 创建备份（v1.0.10）：`miniprogram_v1.0.10_20260608_152649.tar.gz`（5.3MB，排除 node_modules）

---

## 管理后台用户管理页面修复（15:47-15:58）

### 问题1：注册时间显示为空
**原因**：后端返回的数据字段格式可能是下划线格式（`created_at`），但前端期望驼峰格式（`createdAt`）

**修复**：修改 `admin-panel/src/pages/Users/index.tsx`，兼容两种格式：
- 列表页面：添加 `render` 函数，`record.createdAt || record.created_at || '-'`
- 用户详情弹窗：同样兼容两种格式

### 问题2：注册时间格式不方便查看
**原因**：显示格式为 `2026-06-04T03:57:09.204Z`（ISO 格式），不方便阅读

**修复**：添加 `formatTime()` 函数，将 ISO 时间转换为 `YYYY-MM-DD HH:mm:ss` 格式：
```typescript
const formatTime = (isoStr: string): string => {
  if (!isoStr || isoStr === '-') return '-';
  try {
    const d = new Date(isoStr);
    const pad = (n: number) => String(n).padStart(2, '0');
    return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}:${pad(d.getSeconds())}`;
  } catch {
    return isoStr;
  }
};
```

**显示效果**：
- 修复前：`2026-06-04T03:57:09.204Z`
- 修复后：`2026-06-04 11:57:09`

### 部署记录
- 15:58 重新构建管理后台（npm run build）
- 15:58 部署到生产服务器（rsync build/ → /var/www/admin/）

---

## 备份（最新）
2026-06-08 15:58 创建备份（v1.0.10）：`miniprogram_v1.0.10_20260608_155800.tar.gz`

---

## 开启开发模式（20:48）

### 变更
1. `utils/config.js`：`DEV_MODE = true` + `DEV_MOCK_DATA = true`
   - "我的"页头像下方显示身份切换选择器（picker）
   - 各工作台使用 mock 数据，不请求真实后端接口

### 可切换角色（8个）
| 角色 | key | 说明 |
|------|-----|------|
| 会员 | user | 普通会员视角 |
| 访客 | user | 未建档视角 |
| 公益推荐官 | public_matchmaker | 推荐官工作台 |
| 联创推荐官 | partner_matchmaker | 联创推荐官工作台 |
| 城市合伙人 | city_franchisee | 合伙人工作台 |
| 专业推荐官 | professional_recommender | 专业推荐官平台 |
| 社区服务站 | community_station | 服务站工作台 |
| 管理员 | admin | 管理员视角（含推荐码管理） |

### 切换方式
"我的"页面 → 头像下方下拉选择器 → 选择角色 → 自动切换
切换后持久化保存，下次打开自动恢复最后一次选择的角色

---

## 备份（最新）
2026-06-08 22:13 创建备份：`miniprogram_backup_20260608_221319.tar.gz`（5.5MB，排除 node_modules/memory）
