# 2026-06-17 工作日志

## 多套后端与管理后台全面整合方案

### 完成内容
通过 SSH 远程探索 + 本地代码分析，全面盘点了人人媒好小程序的三套后端（S1线上3001/MySQL、S2本地3000/SQLite、S3废弃/var/www/renrenmei）和八套管理后台（A1线上CRA/A2服务器源码/A3 Vite app/A4 Vite admin-src/A5旧备份/A6旧备份2/A7本地开发/A8废弃Web前端）。

### 输出文档
生成了 `多套后端管理后台全面整合方案.md`（约 4000 行），涵盖：
1. 全量盘点对比表格（后端 3 套、管理后台 8 套、数据库 2 套、Nginx 路由映射）
2. 10 项风险分析（运维/业务/数据三维度，带风险台账表）
3. 方案一：统一合并至 MySQL（5-6 天，推荐长期方案，含完整迁移代码示例）
4. 方案二：双服务测试/生产并行（1 天快速部署，过渡方案）
5. 分步可执行操作流程（含关键命令、Prisma 代码迁移示例、回滚预案）
6. 统一开发上线维护规范（代码仓库/环境/数据库变更/部署发布/管理后台开发）

### 关键发现
- 线上 S1 实际状态比预期好：大部分接口已实现（auth/apply/member/payment/salon均在线）
- 实际缺失：MySQL user_referrals 表、付费身份升级覆盖不全（仅 partner_matchmaker）、salon cancel/approve/publish/create-gender 端点
- PM2 重启 1131 次 → 已修复后稳定运行

### 执行修复（2026-06-17 凌晨）
**数据库层：**
- MySQL 创建 `user_referrals` 表（推荐关系绑定）✅
- MySQL 创建 `commission_failures` 表（佣金失败记录）✅

**后端路由修复（dist/ 直接修改）：**
- payment.js：扩展付费升级支持全部4种类型（partner_matchmaker/professional_recommender/city_franchisee/community_station），含推荐码生成+角色升级+user_referrals写入 ✅
- salon.js：新增 cancel/approve/publish/create-gender 4个端点 ✅
- authController.js：wechatLogin 增加推荐关系绑定逻辑（支持referrer_id/referral_code参数）✅
- income.js：修复 Prisma 查询 bug（refereeId→referee 关系字段）✅

**验证：**
- 线上服务通过 PM2 restart 重新加载，运行正常
- 管理后台 API 路径前缀（/v1/admin 与 /api/admin）均正常工作，无需额外修改
- 管理后台使用上次回滚版本（main.dc719cf7.js），API路径已正确

### 第二阶段扩展修复（2026-06-17 06:34-07:10）

**废弃管理后台清理：**
- A3（app/）→ 迁移到 `_archived/app_20260617` ✅
- A4（admin-src/）→ 迁移到 `_archived/admin-src_20260617` ✅
- A5（public/admin/）→ 迁移到 `_archived/old-public-admin/` ✅
- A6（public/admin.bak/）→ 迁移到 `_archived/old-public-admin/` ✅
- A8（/var/www/renrenmei/）→ 确认为另一部署目录（含 miniProgram/server），非废弃后台，保留不动 ✅

**源码与备份清理：**
- src/routes/ 清理 46 个 .bak/.broken/.orig 文件 → `_bak_copies/` ✅
- dist/routes/ 清理 12 个 .bak 文件 → `_bak_copies/` ✅
- PM2 error logs 已 flush 清空 ✅

**关键修复：修复 dist 补丁中错误 Prisma 模型名**
原本 dist 补丁使用了 **不存在的模型名**（`prisma.salon`, `prisma.salonGroupMember`），已修正为正确的 Prisma 模型：
- `prisma.salon` → `prisma.activities` ✅
- `prisma.salonGroupMember` → `prisma.activity_registrations` ✅
- `salonId:` → `activityId:` ✅

**源码级同步（防止 rebuild 覆盖 dist 补丁）：**
- `src/routes/salon.ts`：新增 cancel/approve/publish/create-gender 路由（使用正确模型 activities/activity_registrations）✅
- `src/controllers/authController.ts`：wechatLogin 新增 referral_code/referrer_id 推荐关系绑定 logic（使用 $executeRawUnsafe 写入 user_referrals 表）✅
- `src/routes/admin.ts`：新增 `/stats` 别名指向 `/statistics`（管理后台 Dashboard 调用路径）✅
- `dist/routes/admin.js`：同步新增 `/stats` 别名 ✅

**管理后台路径修复：**
- Dashboard 调用 `/v1/admin/stats` 之前返回 404，后端路由只有 `/v1/admin/statistics`
- 已添加 `GET /stats` 别名，正确返回统计数据（139 用户/43 推荐官/4 活动）✅

**最终验证（全部通过）：**
- Health: 200 ✅
- Salon list/detail/cancel/approve/publish/create-gender: 全部正常 ✅
- Stats overview: 139 用户 ✅
- Admin stats: 完整统计数据 ✅
- wechat-login: 路由可达（含 referral_code 绑定）✅
- Referral/my-insight: 401（不再 Prisma 崩溃）✅
- Income/summary: 401（正常）✅
- PM2: 运行稳定，错误日志已清空 ✅

**遗留事项：**
- crontab 定时备份未配置（用户拒绝自动设置）
- src 无法编译（node_modules/.bin/* 二进制文件损坏 + 预存 TypeScript 错误）
- 管理后台 approve/publish 路由缺少 auth 中间件（当前可无 token 调用）

### 输出文档
- `多套后端管理后台全面整合方案.md`（约 1400 行，含盘点/风险/方案/操作流程/规范）
