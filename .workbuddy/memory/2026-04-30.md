# 2026-04-30 日志

## 内容安全审核修复（继续）

### 已完成
- **chat.js 接入内容安全检测**：私信发送接口 `/v1/chat/:chatId/send` 已接入 `checkTexts`，scene=2（私信消息），在 INSERT 前检测，违规统一提示「发布内容含违规信息，请修改后重试」
- **PM2 重启生效**：renrenmei-api 已重启，服务状态 online
- **小程序端路由已对齐**：contentModeration.js 中 `/moderation/text-check` 与后端 `/v1/moderation/text-check` 已通过 BASE_URL (`https://rrmhdate.cn/v1`) 正确拼接，无需额外修改

### 新增：多媒体安全检测 + 用户安全等级（2026-04-30）

**wechat-security.js 扩展**（完整重写）：
- `imgSecCheck(buffer, filename)` — 同步图片检测（≤1MB，实时返回）
- `mediaCheckAsync(buffer, filename)` — 异步图片检测（≤10MB，含 trace_id + 回调机制）
- `getUserRiskRank(userInfo)` — 用户安全等级查询（0-4级）
- `waitForMediaCheckResult` / `injectMediaCheckResult` — 异步回调配合函数

**moderation.js 扩展**（完整重写）：
- `POST /v1/moderation/image-check` — multipart 文件上传，自动选同步（≤1MB）/异步（>1MB）策略
- `GET /v1/moderation/image-check` — URL 模式图片检测
- `POST /v1/moderation/media-callback` — 微信异步检测回调入口
- `POST /v1/moderation/user-risk-rank` — 用户安全等级查询

**WECHAT_APP_SECRET 已配置**（2026-04-30），微信 API 正式生效

### 查漏补全（2026-04-30 夜）

**服务端新增接入内容安全检测的接口**：
- `user.js` `PUT /profile/update` — 检测 nickname/intro/occupation，scene=1
- `avatar.js` `POST /generate` — 检测 description/personality，scene=1
- `meet.js` `POST /apply` — 检测 remark，scene=2

**小程序端 contentModeration.js 更新**：
- `serverCheckImageUpload(filePath, token)` — 新增 multipart 上传专用函数
- `serverCheckUserRisk(params)` — 新增用户安全等级查询
- `serverCheckImage` 补充说明用法（需配合 wx.uploadFile）

**完整文本检测覆盖场景**：
| 场景 | 接口 | scene |
|------|------|-------|
| 用户资料填写 | `PUT /v1/user/profile/update` | 1 |
| 虚拟人分身创建 | `POST /v1/avatar/generate` | 1 |
| 私信消息发送 | `POST /v1/chat/:chatId/send` | 2 |
| 约见申请备注 | `POST /v1/meet/apply` | 2 |
| 沙龙活动创建 | `POST /v1/salon/create` | 4 |
