# 2026-07-18

## Git 仓库推送修复
- 从版本管理中移除了误跟踪的大文件：`admin-panel/admin-build.tar.gz`（构建产物，~1.9MB）、`database/rrmhdb.sqlite`（本地数据库，~466KB）
- 远程仓库从 HTTPS 切换为 SSH（`git@github.com:MilesJCZhang/rrmh.git`），SSH key 已存在无需新建
- 推送成功，代码已同步到 GitHub main 分支

## 推荐官工作台数据同步修复（接上一项分析）
分析结论：前端工作台↔后端(S2/S1)已完全打通，唯一断层是管理后台缺访客视图，且访客写入因分享链接带 `id=` 而非推荐码 `code=` 导致 visitor_logs 长期 0 行。

已执行（全部增量，不动现有功能）：
1. **小程序分享链接修复**：matchmaker-workbench.js:379 `onShareAppMessage` 由 `id=userId` 改为 `code=insightCodeInfo.code`，访客进首页后可被 parseReferralScene 解析并自动 logVisitor + 绑定推荐官。
2. **管理后台新增访客管理页**：ReferralVisitors/index.tsx + referral-visitor.service.ts + App.tsx 路由 + Layout.tsx 菜单，支持搜索/分页/注册状态筛选。
3. **后端访客列表接口**：S2(routes_referral-codes.js) 与 生产S1(src/routes/admin-referral.ts) 均增量新增 `GET /visitors`，已 `npx tsc` 重新 emit dist + pm2 restart 生效（curl 经 Nginx 返回 401=需鉴权，确认路由注册成功）。
4. **构建部署**：admin-panel build 成功，scp 到 /var/www/admin/，验证 JS 含访客管理页（中文被 Terser 转义为 \u8bbf，已确认）。
5. 提交 `d7b9dad` 并推送 GitHub main。

注意：S1 生产 `npx tsc` 存在项目级既有类型错误（authController/recommendController 等），但 noEmitOnError:false 仍会 emit，我的新增接口未引入新错误。部署采用"重新 tsc emit dist + pm2 重启"而非改 dist 源码，更安全。
