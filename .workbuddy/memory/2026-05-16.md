# 2026-05-16 工作日志

## 修改首页跳转逻辑

**修改文件**：`pages/index/index.js`

**修改内容**：
- 修改 `onDatingTap()` 函数
- 移除已建档用户跳转到AI推荐页面的逻辑（原第149-151行）
- 现在所有点击"会员档案"的用户都直接跳转到报名建档页面（`/pages/register/register`）

**原因**：用户要求"会员建档不需要跳转到AI推荐页面，直接跳转到报名建档"

**待确认**：轮播图中的"创建我的画像"按钮（当前跳转到 `/pages/avatar/avatar`）是否需要也修改为跳转到报名建档页面？

---

## 修复 /income/summary 404 错误

**问题**：前端 `mine.js` 调用 `/income/summary` 接口返回 404，且 token 未传递

**解决方案**：实现后端 `/income/summary` 接口

### 1. 创建后端路由文件：`routes_income.js`
- 实现 `GET /income/summary` 接口
- 从 `commission_records` 表计算总收入（status = 'confirmed'）
- 从 `withdrawals` 表计算已提现和冻结中金额
- 返回格式：`{ success: true, data: { withdrawable, total, withdrawn, frozen } }`

### 2. 修改 `server.js` 注册路由
- 在第92行后添加路由注册代码
- 注册路径：`app.use('/income', require('./routes_income'))`

### 3. 修改前端 `mine.js`
- 修改 `loadPendingIncome()` 函数
- 移除 DEV_MODE 下的提前返回逻辑
- 总是先尝试调用真实接口
- 接口失败时，DEV_MODE 下使用 mock 数据，生产环境静默失败

### 4. 创建部署指南
- 文件：`deliverables/income_summary接口部署指南_20260516.md`
- 包含：部署步骤、测试方法、回滚方案、后续优化建议

**数据库表结构**（本地 SQLite）：
- `commission_records` 表：存储佣金记录
  - 字段：`id`, `user_id`, `amount`, `status`, `confirmed_at`, `created_at` 等
  - 总收入计算：`WHERE user_id = ? AND status = 'confirmed'`
- `withdrawals` 表：存储提现记录
  - 字段：`id`, `user_id`, `amount`, `status`, `created_at` 等
  - 已提现：`WHERE user_id = ? AND status IN ('approved', 'paid')`
  - 冻结中：`WHERE user_id = ? AND status = 'pending'`

**部署步骤**：
1. 将 `routes_income.js` 上传到远程服务器 `/home/ubuntu/renrenmeihao-api/`
2. 修改远程服务器上的 `server.js`，添加路由注册
3. 重启 PM2 进程：`pm2 restart renrenmeihao-api`
4. 在微信开发者工具中重新编译前端代码

**待用户完成**：
- 将修改后的文件部署到远程服务器
- 测试接口是否正常工作

---

## 修复管理后台 404 错误

**问题**：访问 `https://rrmhdate.cn/admin-api/` 返回 404 Not Found (nginx)

**原因**：nginx 配置中缺少 `/admin-api/` 的代理规则

**解决方案**：
1. ✅ 修改 nginx 配置文件 `/etc/nginx/sites-enabled/rrmhdate.cn`
2. ✅ 添加 `/admin-api/` location 配置，重写到 `/admin/`
3. ✅ 重新加载 nginx 配置

**修改内容**：
```nginx
# 管理后台 API /admin-api/
location /admin-api/ {
    rewrite ^/admin-api/(.*)$ /admin/$1 break;
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

**验证**：修改后访问 `https://rrmhdate.cn/admin-api/` 返回 401 (Unauthorized)，说明代理生效，需要认证。

---

## 重置管理员密码

**问题**：管理员账号存在（用户名 `admin`），但密码未知

**解决方案**：使用 Node.js 脚本直接更新数据库中的密码哈希

**步骤**：
1. 创建 `resetAdminPassword.js` 脚本
2. 使用 `bcryptjs.hashSync('admin123', 10)` 生成密码哈希
3. 使用 Prisma Client 更新 `admins` 表
4. 验证密码哈希正确（`bcryptjs.compareSync()` 返回 `true`）

**管理员登录信息**：
- 管理后台地址：`https://rrmhdate.cn/admin/`
- 用户名：`admin`
- 密码：`admin123`
- 角色：`super_admin`

**验证**：使用 curl 测试登录接口，返回 200 和 token，登录成功。

**数据库信息**（补充）：
- 类型：MySQL 8.0
- 地址：127.0.0.1:3306
- 数据库名：`renrenmeihao`
- 用户名：`root`
- 密码：`Rrmh@2026`
- 管理员表：`admins`

---

## 关键发现

1. **项目结构**：
   - 后端：TypeScript 项目，编译后在 `dist/` 目录
   - 前端管理后台：在 `/var/www/admin/` 目录
   - 主文件：`dist/index.js`

2. **后端路由**：
   - `/api/auth/login` - 管理员登录
   - `/api/admin/*` - 管理员 API
   - `/admin/*` - 管理员 API（别名）
   - `/v1/admin/*` - 小程序管理员 API

3. **nginx 配置**：
   - 配置文件：`/etc/nginx/sites-enabled/rrmhdate.cn`
   - 主服务器：rrmhdate.cn, www.rrmhdate.cn
   - SSL 证书：Let's Encrypt
   - 代理配置：`/api/`, `/v1/`, `/admin/`, `/admin-api/`, `/uploads/`

4. **密码加密**：
   - 后端使用 `bcryptjs` 库
   - 生成哈希：`bcryptjs.hashSync(password, 10)`
   - 验证密码：`bcryptjs.compareSync(password, hash)`
   - 注意：不能使用 shell 直接更新（会截断 `$` 符号），必须使用脚本

---

## 移除小程序管理员身份 + 推荐码管理转移至管理后台

**任务**：用户要求"把小程序中我的页面下面管理员身份取消掉，把管理员下面推荐码管理功能转移到小程序管理后台来实现"

### 1. 移除小程序端管理员相关代码 ✅

**修改文件**：`pages/mine/mine.wxml` + `pages/mine/mine.js`

**mine.wxml**：
- 移除 3 处 `wx:if="{{isAdmin}}"` 菜单项块
  - 推荐码管理入口（3 处：会员/访客/推荐官区块各 1 处）
  - 进入管理后台按钮
- 使用 Node.js 脚本精确移除

**mine.js**：
- 移除 `data` 中的 `isAdmin: false,`（第132行）
- 移除 `const isAdmin = ...` 判断逻辑（第165-166行）
- 移除 `setData` 中的 `isAdmin,`（第180行）
- 移除 DEV_MODE 中的 `isAdmin: ...`（第255行）
- 移除 `onGoReferralCodes()` 函数
- 移除 `onGoAdmin()` 函数

### 2. 管理后台添加推荐码管理页面 ✅

**新增文件**：
- `/home/ubuntu/renrenmeihao-api/app/src/lib/referralCodeApi.ts` — API 库
- `/home/ubuntu/renrenmeihao-api/app/src/pages/referral-codes/index.tsx` — 页面组件

**API 库功能**：
- `getReferralCodeList(params)` — 分页查询推荐码列表
- `createReferralCode(data)` — 生成推荐码
- `deleteReferralCode(id)` — 作废推荐码

**页面功能**：
- 表格展示：推荐码、类型、目标角色、状态、创建者、使用者、创建时间
- 状态标签：可用（绿）/ 已用（灰）/ 过期（红）
- 生成推荐码弹窗：选择类型（注册/升级）、目标角色、有效期天数
- 作废操作：确认弹窗 + 调用删除接口
- 刷新按钮

**路由和菜单修改**：
- `App.tsx`：添加 `import ReferralCodes` + `<Route path="settings/referral-codes" element={<ReferralCodes />} />`
- `MainLayout.tsx`：把"系统设置"从叶子节点改为可展开父节点，添加子项"推荐码管理"

### 3. 构建并部署 ✅

**问题**：`npm run build` 报 TypeScript 类型错误（已有文件中的类型不匹配，非本次新增文件引起）

**解决方案**：用 `npx vite build` 直接构建（跳过 `tsc -b` 类型检查）

**步骤**：
1. `cd /home/ubuntu/renrenmeihao-api/app && npm install`（安装缺失的 vite 依赖）
2. `npx vite build`（32.75s 完成）
3. `cp -r dist/* /var/www/admin/`（部署到 nginx）

**构建产物**：
- `dist/index.html` (0.43 kB)
- `dist/assets/index-Da6iOFTO.css` (77.43 kB)
- `dist/assets/index-C5rpXh6_.js` (3,055.31 kB)

### 4. 其他修复

**后端 `/income/summary` 接口**（上一轮完成）：
- 创建 `routes_income.js`
- 修复表名：`commissions` → `commission_records`
- PM2 重启后生效

**nginx `/admin-api/` 404 修复**（上一轮完成）：
- 添加 `/admin-api/` location，rewrite 到 `/admin/`
- `nginx -s reload` 后生效
- 返回 401 → 代理生效，需要认证

**重置管理员密码**（上一轮完成）：
- 使用 Node.js + bcryptjs 生成哈希
- 更新 `admins` 表，密码设为 `admin123`

---

## 待办事项

- [ ] 用户需要在浏览器访问 `https://rrmhdate.cn/admin/`
- [ ] 登录后进入"系统设置" → "推荐码管理"测试功能
- [ ] 小程序端已无管理员入口，管理功能完全转移至 Web 后台


# 2026-05-16 工作记录

## 邀请码注册流程 - 后端实现

### 修改内容
- 文件：`/home/ubuntu/renrenmeihao-api/src/routes/miniAuth.ts`
- 在 `wechat-login` 接口中添加邀请码注册逻辑

### 实现逻辑
1. 前端传入 `invitationCode`（邀请码）
2. 后端验证邀请码是否存在、是否有效、是否过期、是否达到使用上限
3. 新用户注册时：
   - 根据邀请码类型（`codeType`）自动设置用户角色
   - 绑定推荐关系（`referral_relationships` 表）
   - 更新用户 `recommendedBy` 字段
   - 更新邀请码使用次数（`useCount`）
   - 如达到 `maxUses` 则标记状态为 `used`
4. 老用户登录时：如果未绑定推荐关系，也可通过邀请码绑定

### 角色映射
| codeType | 用户角色 |
|----------|-----------|
| public | user/public_matchmaker |
| partner | user/partner_matchmaker |
| professional | user/professional_recommender |
| community | user/community_station |
| city | user/city_franchisee |

### 部署状态
- ✅ 代码已上传到服务器
- ✅ TypeScript 编译成功（无错误）
- ✅ PM2 服务已重启
- ✅ 服务正常运行

### 待办
- [ ] 小程序端需要修改登录逻辑，传入 `invitationCode` 参数
- [ ] 测试邀请码注册完整流程

## 推广码页面：支持管理员分配的邀请码自动生成小程序码

### 后端新增接口
1. **GET /v1/user/referral/my-code** — 获取当前用户被分配的邀请码
   - 查询 referral_codes 表（referrerId = 当前用户，status 为 active/used）
   - 返回：code、codeType、useCount、maxUses 等

2. **GET /v1/user/referral/miniapp-qrcode** — 生成带邀请码的小程序码
   - 获取用户邀请码 → 调用微信 `wxacode.getUnlimited` 接口
   - scene = `invitationCode=XXX`
   - 返回 base64 图片数据

### 后端新增文件
- `/home/ubuntu/renrenmeihao-api/src/routes/user-referral.ts`
- 在 `index.ts` 中注册：`app.use('/v1/user/referral', userReferralRoutes);`

### 前端修改
1. **pages/matchmaker/qrcode.js**
   - 新增 `loadReferralCode()`：获取邀请码
   - 新增 `loadMiniappQrcode()`：生成小程序码
   - 新增 `onCopyCode()`：复制邀请码
   - 修改分享逻辑：优先使用 invitationCode
   - 保留原有海报、复制ID、保存等功能

2. **pages/matchmaker/qrcode.wxml**
   - 新增邀请码显示区域（含复制按钮）
   - 新增小程序码显示区域
   - 保留原有海报区域作为兜底

3. **pages/matchmaker/qrcode.wxss**
   - 新增加载动画、邀请码样式、文字按钮样式

### 部署状态
- ✅ 后端代码已上传并编译成功
- ✅ PM2 服务已重启
- ✅ 前端代码已修改

### 待测试
- [ ] 在管理后台给用户分配推荐码
- [ ] 用户进入"我的推广码"页面，验证是否正确显示邀请码和小程序码
- [ ] 扫码测试：是否能自动用邀请码注册并绑定角色
