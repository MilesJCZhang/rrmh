# 2026-06-01 工作日志

## 管理后台审核功能迁移

### 目标
将推荐官审核功能从 `Officers.tsx` 统一迁移到 `referral-codes/index.tsx`，使逻辑更清晰。

### 完成内容

#### 1. `referralCodeApi.ts` 追加 API 函数
- 新增 `getPendingOfficerList()` — 获取待审核推荐官列表（调用已有 `/admin-api/officers?status=0`）
- 新增 `reviewOfficer()` — 审核通过/拒绝（调用已有 `/admin-api/officers/:id/review`）
- 新增类型：`PendingOfficer`、`PendingOfficerListResult`

#### 2. `referral-codes/index.tsx` 新增"待审核"tab
- `TabKey` 类型增加 `'pending'`
- 新增 state：`pendingData`、`pendingTotal`、`pendingLoading`、`pendingPage`、`pendingPageSize`
- 新增 `fetchPendingData()` 函数
- 新增 `handlePendingReview()` 审核处理函数（通过/拒绝）
- 新增 `pendingColumns` 定义（申请人、推荐官类别、微信号、手机号、申请时间、操作）
- `tabItems` 增加 `{ key: 'pending', label: '待审核' }`
- JSX 中根据 `activeTab` 切换渲染 `pendingColumns` 表格或原 `columns` 表格

#### 3. `Officers.tsx` 移除审核功能
- 移除 `reviewOfficer` import
- 删除 `handleReview()` 函数
- 操作列简化为只有"查看"按钮
- 移除"待审核"统计卡片

### 部署
- 构建成功，JS 文件名：`index-1xRSiEYw.js`、`index-Djn3HGTR.js`
- 部署路径：`/var/www/admin/`
- 清理旧资源：`sudo rm -f /var/www/admin/assets/index-*.{js,css}`

### 待确认
- [ ] 用户需在后台"推荐码管理"页面确认"待审核"tab 正常显示
- [ ] 审核通过/拒绝功能是否正常
- [ ] `Officers.tsx` 页面是否正常（无审核按钮）
