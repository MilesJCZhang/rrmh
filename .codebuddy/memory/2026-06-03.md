# 2026-06-03 工作日志

## 沙龙前端页面更新
- 修复 `salon-list` 页面头部徽章硬编码"6人" → 动态 `{{totalCap}}人`（默认27）
- 修复卡片分组条硬编码 `wx:for="{{3}}"` → `wx:for="{{item.max_per_gender || 3}}"`
- 新增推荐官主体沙龙（非 grouped）的卡片容量展示：单性别名额 + 27人封顶提示
- 修复 `salon-detail` 页面的规格卡片、容量进度条、参与须知文案等硬编码问题
- 统一处理性别型沙龙（male_salon/female_salon）和常规混合沙龙（mixed）的不同展示

## 推荐官沙龙报名页面区分（新增）
- **核心需求**：推荐官沙龙是单性别沙龙，男士进男沙龙，女士进女沙龙，**无**3男3女配对概念；常规沙龙（mixed）才是3男3女6人小班
- `salon-detail.wxml`：
  - 3男3女分组可视化区域增加 `wx:if="{{salon.isGrouped && !salonType}}"` 条件，**推荐官沙龙不显示**3男3女
  - 新增推荐官沙龙单性别名额展示区域（`wx:if="{{salonType}}"`），显示单性别名额 + 封顶提示
- `salon-detail.js`：
  - data 新增 `recommenderSlots`（数组）、`recommenderRemain`（剩余名额）
  - `loadDetail` 新增推荐官沙龙名额计算逻辑（`max_recommenders` - `male_count`）
  - 报名限制逻辑新增性别匹配检查：男沙龙仅男士可报，女沙龙仅女士可报
- `salon-detail.wxss`：新增 `.gv-cap-tip` 样式（单性别名额提示）
- `salon-list.wxml`：推荐官沙龙卡片名额区域改为单性别显示（"男士"/"女士"），不显示男女双栏
- `salon-list.wxss`：已有 `.sc-cap-tip` 样式，无需修改

## 沙龙报名页面信息一致性与版面优化（新增）
- **时间格式化修复**：新增 `formatTime()` 函数，将 ISO 格式 `T01:00:00.000Z` 转为友好的 `01:00` 格式
- **"名额已满"逻辑修复**：推荐官沙龙用 `recommenderRemain` 而非 `maleRemain+femaleRemain` 判断（两字段在推荐官沙龙中始终为0）
- **报名按钮文本修复**：同步使用 `salonRemain` 通用字段判断名额状态
- **费用显示优化**：免费活动显示绿色"免费"而非 "¥0"
- **标签简化**：推荐官名额标签从"男推荐官/女推荐官"改为"男士/女士"
- **WXSS**：新增 `.price-free` 绿色免费样式

## 管理后台活动审核页面
- 新建后端路由 `routes_admin_activities.js`：`GET /v1/admin/activities` 活动列表 API（分页、状态筛选、关键字搜索）
- 在 `server.js` 中挂载新路由
- 新建管理后台页面 `Activities/index.tsx`：Table 列表展示所有沙龙活动，支持按状态/关键字筛选，详情弹窗，待审核活动可通过/拒绝操作
- 新建 `activity.service.ts`：`getActivities()` 和 `approveActivity()` 两个 API 调用
- 在 `App.tsx` 中添加 `/activities` 路由
- 在 `Layout.tsx` 侧边栏菜单添加"活动管理"菜单项（CalendarOutlined 图标）
- 已成功重新构建 admin-panel（build/ 目录已更新）

## 建档资料性别补充与推荐官资料入口
- **partner-apply.js**：城市合伙人(franchisee)和社区服务站(community)入驻申请表单增加`gender`性别字段（必填），专业推荐官(professional)已有
- **auth.service.js**：`syncUserData` 新增 `user.gender` 同步到本地缓存
- **app.js**：`_syncUserData` 新增 `globalData.gender` 同步
- **register.js**：建档提交成功后同步 `gender` 到 `authService` 缓存
- **index.js**：首页推荐官沙龙入口(`onGenderSalonTap`)增加资料完整度检查，后台分配的推荐官点击时若资料不完整，引导至注册页补充资料（含性别）

## 生产服务器apply.ts更新
- **问题**：生产服务器 apply.ts 的 4 个申请处理接口字段与前端不匹配，导致500错误
- **修复**：重写 `apply.ts`，使以下路由接收前端表单字段并正确写入数据库：
  - `/v1/apply/city-franchisee`：接收 company_name/contact_name/gender/phone/city 等字段，写入 franchisees 表，性别同步到 users 表
  - `/v1/apply/community-station`：接收 org_type/org_name/address/gender/phone 等字段，写入 stations 表，性别同步到 users 表  
  - `/v1/apply/professional-recommender`：接收 gender 字段同步到 users 表
  - `/v1/apply/partner-matchmaker`：不变
  - `/v1/apply/public-matchmaker`：不变
- **关键逻辑**：`genderToDb()` 将前端字符串（male/female/男/女）转为数据库数字（1/0），写入 `users.gender`
- **部署**：已编译 TypeScript 并重启 PM2 生效
