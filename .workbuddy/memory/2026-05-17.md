# 2026-05-17 工作记录

## 推荐码绑定链路 - 已验证通过

推荐码 `LCRG4HGL` 绑定访客流程已完整跑通，管理后台 `useCount` 从之前数值更新到 `3 / ∞`：

1. ✅ `/v1/user/referral/verify` 验证推荐码（200 + `valid: true`）
2. ✅ `/v1/referral/bind` 绑定推荐关系（200，数据库 `recommendedBy` 已更新）
3. ✅ 管理后台推荐码列表 `useCount` 同步增加
4. ✅ 前端 `syncUserData()` 已支持读取 `recommendedBy` 字段（兼容 `referrerId`）

## 修复清单
- 后端 `/v1/user/profile` select 中增加 `recommendedBy: true`
- 前端 `auth.service.js` `syncUserData()` 读取 `recommendedBy` 作为 `referrerId`
- 数据库清理测试用户 ID=30 的脏数据 `recommendedBy = NULL`
- 后端 TypeScript 编译通过，PM2 重启生效

## 管理后台用户列表增强
- ✅ `userController.ts` 用户列表 `select` 增加 `wechatAccount: true` 和 `openid: true`
- ✅ `userController.ts` `formatted` 输出增加 `wechatAccount` 和 `openid` 字段
- 管理员现在可以在用户列表直接看到用户的 `openid`，用于分配推荐码

## 推荐码分配链路确认
- ✅ 用户微信登录 → `openid` 写入数据库
- ✅ 管理后台生成不同身份推荐码（`/api/admin/referral-codes/generate`）
- ✅ 管理后台通过 `openid` 分配推荐码（`/api/admin/referral-codes/assign` 支持 `wechat_account` 参数，内部已支持 `openid` 搜索）
- ✅ 推荐码绑定后 `useCount` 正确递增

## 管理后台「所有用户」页面新建
- ✅ 新建 `src/pages/users/AllUsers.tsx` — 所有用户列表页面
  - 表格包含：ID、用户信息、OpenID、微信号、角色、状态、注册时间、操作
  - OpenID 列 `width: 220`，`wordBreak: break-all` 防止长串溢出
  - 支持按昵称/姓名搜索、状态筛选、分页
- ✅ `App.tsx` 增加 `/users/all` 路由
- ✅ `MainLayout.tsx` 菜单「用户管理」下增加「所有用户」入口（排第一位）
- ✅ `npm run build` 构建成功（57s，3.1MB index.js）
- ✅ 构建产物已部署到服务器 `/home/ubuntu/renrenmeihao-api/public/admin/`
- ⚠️ 发现 nginx `location /admin/` 的 `root` 指向 `/var/www`，实际服务目录是 `/var/www/admin/`
- ✅ 已把构建产物从 `/home/ubuntu/.../public/admin/` 复制到 `/var/www/admin/`，并修正属主为 `www-data`
- 强制刷新浏览器后「用户管理」菜单下应显示「所有用户」入口

## 管理后台前端 code 兼容修复
- 后端 `success()` 返回 `code: 200`，但管理后台前端所有 API 文件检查 `code !== 0` 视为失败
- 导致「所有用户」页面 API 请求成功但前端抛错，表格显示「暂无数据」
- ✅ 已修改 `userApi.ts`、`verifyApi.ts`、`financeApi.ts`、`activityApi.ts` 共 5 处检查逻辑为 `code !== 0 && code !== 200`
- ✅ 重新构建并部署到 `/var/www/admin/`

## 待跟进
- `/v1/config/public-map` 仍缺失（commissionRules.js 依赖）
- `/v1/stats/overview` 仍缺失（首页 stats 依赖）

## 2026-05-17 晚间补充（authController 修复 + 管理员登录打通）

### authController.ts 修复
- 文件被之前 `sed` 误插入 `console.log` 导致语法损坏，TypeScript 编译失败
- ✅ 完整重写 `authController.ts`，包含全部 8 个导出（login/me/register/userLogin/wechatLogin/getUserInfo/updateUserInfo/bindPhone）
- ✅ `npx tsc` 编译零错误，PM2 已重启

### 管理员密码修复
- 数据库里的密码哈希是用 `bcrypt`（C++）生成的，`bcryptjs`（纯 JS）无法比对
- ✅ 用 Node.js 脚本通过 Prisma 直接更新，生成 `bcryptjs` 兼容哈希
- ✅ `/api/auth/login` 返回真实 JWT（code: 200）

### 前端 admin API 路径修复
- 根本原因：`userApi.ts`、`verifyApi.ts` 等文件中，管理后台 API 调用用了 `API_BASE_URL = '/v1'`，实际后端路径在 `/api` 下
- ✅ `userApi.ts`：添加 `adminRequest()`，更新 `getAdminUserList`/`updateAdminUserStatus`/`getMatchmakerList`/`getSingleMemberList`/`getOfficerList`/`getStationList`/`getFranchiseeList`/`importMatchmakers` 共 8 处
- ✅ `verifyApi.ts`：添加 `adminRequest()`，更新 4 处认证审核 API
- ✅ 重新构建并部署到 `/var/www/admin/`
- ⚠️ `financeApi.ts`、`activityApi.ts`、`referralCodeApi.ts` 尚未修复，需要继续

### 浏览器端操作要求
- 清除 LocalStorage 中的旧 mock token（`mock_token_xxx`）
- 用 `admin` / `123456` 重新登录
- 硬刷新（Cmd+Shift+R）绕过前端缓存

## 2026-05-18 凌晨补充（全面打通确认）

### 前端 admin API 路径修复（第二批）
- ✅ `financeApi.ts`：`API_BASE_URL` 从 `/v1` 改为 `/api`
- ✅ `activityApi.ts`：`API_BASE_URL` 从 `/v1` 改为 `/api`
- ✅ `referralCodeApi.ts`：复用 `../utils/request`（baseURL 已是 `/api`），无需修改
- ✅ 构建并部署到 `/var/www/admin/`

### 后端 matchmaker 路由补全
- `matchmaker.ts` 只有 `/:id/info`，缺少 `/list` 和 `/status`
- ✅ 重写 `matchmaker.ts`，新增 `GET /list`（分页+搜索）和 `PUT /:id/status`
- ✅ 编译零错误，PM2 已重启

### 小程序前端 API 全面检测
批量测试所有小程序调用的接口，结果：**零 404**
- `/v1/config/public-map` ✅ 200
- `/v1/stats/overview` ✅ 200
- `/v1/matchmaker/list` ✅ 200（刚修复）
- `/v1/user/profile` ✅ 401（需登录，路由存在）
- `/v1/auth/wechat-login` ✅ 400（需 code，路由存在）
- `/v1/payment/create` ✅ 400（路由存在）
- `/v1/verify/submit` ✅ 400（路由存在）
- 所有其他接口均返回 200/401/400，无 404

### 上线前状态总结
- ✅ 后端所有 API 路由已打通
- ✅ 管理后台登录 + 用户列表正常
- ✅ 小程序前端所有接口无 404
- ⚠️ 小程序端需实测微信登录流程（DevTools）
- ⚠️ 微信审核需要：隐私协议页、用户协议页、内容安全审核
