# 2026-06-09 工作日志

## 仪表盘UI优化
- 安装 recharts 图表库，实现分数段柱状图和等级占比环形饼图
- 统计卡片改用渐变色背景 + 悬浮上移 + 阴影效果
- 新增刷新按钮 + 上次更新时间显示
- Skeleton 骨架屏加载，代替 Spin 整页居中
- 评分分布增加进度条 + 百分比显示
- 数据加载失败时显示 Empty 状态 + 重试按钮
- 新建 index.scss 自定义样式文件
- 安装 sass 依赖以支持 SCSS 编译

## 基金托管列表修复
- **问题1**：前端 `fund-custody.service.ts` 使用 `/api/admin/fund-custody`，但生产后端将 `admin-premium` 挂载在 `/v1/admin` 下（没有挂载 `/api/admin`）
  - 修复：改成 `/v1/admin/fund-custody`
- **问题2**：生产后端 `admin-premium.ts` SQL 查询使用 `fc.userId`（MySQL 实际列名是 `user_id`）
  - 修复：改成 `fc.user_id`
- **问题3**：生产后端统计 SQL 别名 `totalAmount`（前端读 `total_amount`）
  - 修复：改成 `total_amount`
- 同时修复了 `dist/routes/admin-premium.js`（编译后文件）和 `src/routes/admin-premium.ts`（源码）
- PM2 重启后端服务使修改生效
